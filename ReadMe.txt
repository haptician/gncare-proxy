# gncare-proxy
# timesheet & clock-in/out
# 06/10/2026

# $git add <filename> (e.g. git add public)
# $git commit -m "<comment>" (e.g. git commit -m "confirmation window")
# $git push origin main
# Render's Dashboard > Manual Deploy > Clear build cache & deploy

